version: '3.8'

services:
  bullmq:
    build:
      context: .
    container_name: bullmq
    command: ["yarn", "bullmq"]
    networks:
      - app-network
    ports:
      - "3001:3001"
    environment:
      - ./apps/next-app/.env.production
    restart: always

networks:
  app-network:
    driver: bridge
