services:
  nextjs:
    image: node:20-alpine3.19
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - '3008:3008'
    environment:
      - NODE_ENV=development
    env_file:
      - ./apps/next-app/.env.development
    command: >
      sh -c "yarn devdev"
    networks:
      - app-network

volumes:
  www-html:

networks:
  app-network:
    driver: bridge
