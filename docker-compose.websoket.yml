services:
  websocket-server:
    image: node:20-alpine3.19
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - '4000:4000'
    env_file:
      - ./apps/next-app/.env.production
    command: >
      sh -c "yarn webS"
    networks:
      - app-network

volumes:
  www-html:

networks:
  app-network:
    driver: bridge
