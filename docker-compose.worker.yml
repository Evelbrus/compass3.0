services:
  worker:
    container_name: worker
    environment:
      - ./apps/next-app/.env.production
    command: >
      sh -c "yarn worker"
    networks:
      - app-network
    restart: always

volumes:
  www-html:

networks:
  app-network:
    driver: bridge
