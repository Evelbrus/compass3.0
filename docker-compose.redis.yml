version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - '6380:6379'
    env_file:
      - ./apps/next-app/.env.production
    networks:
      - app-network
    restart: always

networks:
  app-network:
    driver: bridge
